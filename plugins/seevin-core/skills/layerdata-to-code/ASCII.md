## ASCII 绘图

### 绘图符号

| 符号 | 用途       |
| ---- | ---------- |
| `+`  | 角落连接点 |
| `-`  | 水平线     |
| `\|` | 垂直线     |
| ` `  | 填充区域   |

### 绘图规则

1. 使用图层的 `content` 作为布局区域名称,如果为空,则使用 `name` 字段
2. 保持相对比例关系
3. 清晰显示嵌套层级
4. 标注主要尺寸 (可选)

### 草图用途

- 帮助开发者快速理解页面结构
- 确认布局关系是否正确
- 作为代码生成的参考蓝图
